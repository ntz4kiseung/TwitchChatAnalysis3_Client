import React from 'react';

const StreamerStatusItem = ({title, value}) => (
    <div class="">
        <div>
            {title}
        </div>
        <div>
            {value}
        </div>
    </div>
);

export default StreamerStatusItem;